"use client";

import Link from "next/link";
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";
import { ArrowLeft, Download, Dumbbell } from "lucide-react";

export default function WorkoutResultPage() {
  const workoutPlan = typeof window !== "undefined"
    ? localStorage.getItem("workoutPlan")
    : "";

  return (
    <main className="min-h-screen bg-black text-white">
      <div className="fixed inset-0 -z-10">
        <div className="absolute top-20 left-1/2 -translate-x-1/2 w-[500px] h-[500px] rounded-full bg-primary-400/10 blur-[140px]" />
      </div>

      <header className="border-b border-white/5 backdrop-blur-xl">
        <div className="max-w-6xl mx-auto px-4 py-5 flex items-center justify-between">
          <Link
            href="/workout-generator"
            className="flex items-center gap-2 text-white/70 hover:text-primary-400 transition"
          >
            <ArrowLeft size={18} />
            Back
          </Link>
          <div className="font-black text-xl">
            Gym<span className="text-primary-400">Gen</span> AI
          </div>
          <div />
        </div>
      </header>

      <section className="max-w-4xl mx-auto px-4 py-12">
        <div className="inline-flex items-center gap-2 bg-primary-400/10 border border-primary-400/20 rounded-full px-4 py-2 mb-6">
          <Dumbbell size={16} className="text-primary-400" />
          <span className="text-xs tracking-wider uppercase">
            AI Generated Workout
          </span>
        </div>

        <h1 className="text-4xl md:text-5xl font-black mb-4">
          Your Personalized
          <span className="block text-primary-400">Workout Plan</span>
        </h1>

        <p className="text-white/60 mb-10">
          Generated by GymGen AI using Gemini, based on your fitness profile.
        </p>

        <div className="bg-white/[0.02] border border-primary-400/20 rounded-3xl p-6 md:p-10 backdrop-blur-sm">
          {workoutPlan ? (
            <ReactMarkdown
              remarkPlugins={[remarkGfm]}
              components={{
                h1: ({ ...props }) => <h1 className="text-2xl md:text-3xl font-bold text-primary-400 mb-6 mt-0" {...props} />,
                h2: ({ ...props }) => <h2 className="text-xl md:text-2xl font-semibold text-white mt-6 mb-3" {...props} />,
                h3: ({ ...props }) => <h3 className="text-lg font-semibold text-white/90 mt-4 mb-2" {...props} />,
                p: ({ ...props }) => <p className="text-white/80 mb-3 leading-relaxed" {...props} />,
                ul: ({ ...props }) => <ul className="text-white/80 mb-3 space-y-1 list-disc list-inside" {...props} />,
                ol: ({ ...props }) => <ol className="text-white/80 mb-3 space-y-2 list-decimal list-inside" {...props} />,
                li: ({ ...props }) => <li className="mb-1" {...props} />,
                strong: ({ ...props }) => <strong className="text-white font-semibold" {...props} />,
              }}
            >
              {workoutPlan}
            </ReactMarkdown>
          ) : (
            <p className="text-white/60">No workout plan generated yet. Go back to generate one!</p>
          )}
        </div>

        <div className="flex flex-wrap gap-4 mt-8">
          <Link
            href="/workout-generator"
            className="px-6 py-3 border border-white/10 rounded-xl hover:border-primary-400/30 transition"
          >
            Generate Another Plan
          </Link>
          <button
            onClick={() => window.print()}
            className="bg-primary-400 text-black px-6 py-3 rounded-xl font-semibold flex items-center gap-2 hover:bg-primary-300 transition"
          >
            <Download size={18} />
            Download / Print
          </button>
        </div>
      </section>
    </main>
  );
}
